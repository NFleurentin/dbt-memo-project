version: 2

sources:
  - name: raw
    tables:
      - name: customers
        config:
          external_location: "{{ env_var('CUSTOMER_PATH') }}"
      - name: orders
        config:
          external_location: "{{ env_var('ORDER_PATH') }}"
